const mainStore = {
  state: {
    courseList: []
  },
  getters: {
    fetchCourseList(state) {
      return state.courseList;
    }
  },
  mutations: {
    
  },
  actions: {
    FETCH_COURSE_LIST(context) {
      return 
    }
  }
};

export default mainStore;
